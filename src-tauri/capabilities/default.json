{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal permissions for the kiosk window and native command bridge",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default",
    "opener:default",
    "dialog:default",
    "mcp-bridge:default"
  ]
}
