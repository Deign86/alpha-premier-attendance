task-11 FAIL QA — Kill mid-PROCESSING / mid-batchUpdate (fixture, no live Sheets)
date: 2026-09-18 | todo: 11
DISCLOSE: no live app/kill harness or Sheets creds on this box — kill paths
  proven at the decision + recovery-SQL layer (the same code run_once and
  the manual batch call), per the todos 1-10 loader-proxy pattern.

FAILING-FIRST (pre-fix HEAD ae82073, measured):
  git show HEAD:.../dtr_sync.rs | grep -c resume_idempotent → 0
    (no restart-resume proof existed: filter `cargo test resume_idempotent`
    matched 0 tests = FAIL, task-8 precedent).
  git show HEAD:.../sheets_sync.rs → line 44
    `const PROCESSING_LEASE_TIMEOUT_MINUTES: i64 = 5;` + line 835 inline
    `Duration::minutes(...)` (hardcoded 5-min, no cfg override → a 3s-old
    PROCESSING lock reads fresh → victim row stuck, never RETRY in-test).
  git show HEAD:.../sync_retry.rs | grep -c processing_lease_stale_secs → 0.
  Post-fix gap demo (executing harness): stale_secs(3s-old, lease 300) ==
    false (stuck pre-fix) vs stale_secs(3s-old, lease 2) == true
    (recovered post-fix) — same predicate, both horizons, green.

FAIL SCENARIO 1 — kill-mid-PROCESSING → must be RETRY, not stuck/DEAD:
  Mechanism: run_once → recover_stale_processing_leases (stale → RETRY,
  attempts preserved, next_attempt_at=now) BEFORE the should_dispatch
  early-return, so recovery fires even with no Google configured.
  Proven by `resume_idempotent`: 5 PENDING + 1 PROCESSING locked 3s ago +
  dead endpoint tick → CHECK victim RETRY, zero PROCESSING/SYNCED/DEAD,
  lease_recovered==1. Compile-verified (cargo test --lib --no-run
  Finished); execution DISCLOSE: lib harness exit 0xc0000139 on this box
  (pre-existing, see happy log). dtr_pending row asserted present after
  the tick (survives restart; no boot wipe exists — only targeted deletes).

FAIL SCENARIO 2 — kill mid-batchUpdate → retry applies missing ranges only:
  Mechanism: values:batchUpdate overwrites B:E in place (no append
  semantics), and every retry re-plans from live tab reads —
  plan_dtr_push_in_rows returns InSync for already-written days
  (skip-identical arm, dtr_sync.rs) so only missing days produce Write
  plans. Proven in `resume_idempotent` pure section: same plan replays to
  byte-identical USER_ENTERED bodies (1 range); after simulating the 9/5
  landing, 9/5 re-plans InSync (no duplicate B:E write) while 9/4 still
  Writes. Same execution DISCLOSE as scenario 1; body-shape + boundary
  logic additionally covered by the executing sync_retry harness family.
