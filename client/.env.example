# Empty uses the current browser origin, so a viewer opened from a LAN IP stays on that host.
VITE_API_BASE_URL=
# Optional Vite proxy target when the API runs on another local host/port.
VITE_API_PROXY_TARGET=http://127.0.0.1:3001
